# Supabase Environment Template
# Copy to .env.local (local dev) or provide values in your deployment platform.

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Supabase project endpoint (e.g., https://xyzcompany.supabase.co)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Public anon key (safe for browser usage)
VITE_SUPABASE_ANON_KEY=your-anon-key

# Service role key (server-side scripts only)
# Do NOT expose this in the browser. Required for CLI scripts that write data.
VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Primary asset bucket used by the image helper
VITE_SUPABASE_STORAGE_BUCKET=directus-uploads

# Optional flags
VITE_ENABLE_DEBUG_LOGGING=true

# =============================================================================
# OPTIONAL: LOCAL DEVELOPMENT SHORTCUTS
# =============================================================================

# When using `supabase start`, point to the local stack:
# VITE_SUPABASE_URL=http://localhost:54321
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# VITE_SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# OPTIONAL: SCRIPT/TEST CONFIG
# =============================================================================

# Provide the non-prefixed variant if you prefer (`SUPABASE_SERVICE_ROLE_KEY`).
# Scripts will look for either name before failing.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Set to `true` to enable verbose logging in CLI scripts.
# ENABLE_SCRIPT_DEBUG=true
